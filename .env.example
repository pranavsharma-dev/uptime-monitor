DB_HOST=db
DB_PORT=5432
DB_NAME=uptime
DB_USER=pranav
DB_PASSWORD=change-me

# Connection retry tuning for get_connection() in app/db.py
DB_MAX_RETRIES=10
DB_RETRY_DELAY=2

# Set to 1 to disable the APScheduler background job (used by the test suite)
DISABLE_SCHEDULER=0
