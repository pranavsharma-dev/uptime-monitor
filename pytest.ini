[pytest]
; Ensures `app` is importable when pytest is invoked directly (not via
; `python -m pytest`), since the plain `pytest` entry point does not add
; the current directory to sys.path on its own.
pythonpath = .
